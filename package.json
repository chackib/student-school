{
  "name": "school-management-api",
  "version": "1.0.0",
  "description": "REST API for managing schools and students with MongoDB",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo 'No build step required'",
    "docker:build": "docker build -t school-management-api .",
    "docker:run": "docker run -p 3000:3000 school-management-api",
    "docker:compose": "docker-compose up -d"
  },
  "keywords": ["nodejs", "express", "mongodb", "rest-api", "crud"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "body-parser": "^1.20.2",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5",
    "compression": "^1.7.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
